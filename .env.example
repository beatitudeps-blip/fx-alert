# Twelve Data API キー（必須）
# https://twelvedata.com/ で取得
TWELVEDATA_API_KEY=your_api_key_here

# LINE通知設定（本番送信時のみ必須、dry-runでは不要）
LINE_CHANNEL_ACCESS_TOKEN=your_line_token_here
LINE_USER_ID=your_line_user_id_here

# 注意：
# - このファイルを .env にコピーして実際の値を設定してください
# - .env は .gitignore に含まれています（コミットされません）
# - cron実行時は、環境変数が読み込まれないため、crontab内で直接設定するか、
#   絶対パスで .env を読み込むスクリプトを用意してください
